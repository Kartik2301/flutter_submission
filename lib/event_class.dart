class event_class {
  bool identifier; // true in this case
  String name;
  String url;
  String category;
  event_class(bool identifier, String name, String url, String category) {
    this.identifier = identifier;
    this.name = name;
    this.url = url;
    this.category = category;
  }
}